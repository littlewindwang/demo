import {Box, Button} from "@mui/material";


function SearchResultItem({item}) {
    // console.log(item)
    return (
        <Box>
            this is reach SearchResultItem {item.objectID}
        </Box>
    );
}

export default SearchResultItem;
